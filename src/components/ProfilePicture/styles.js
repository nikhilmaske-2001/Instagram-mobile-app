import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    container: {
        margin: 5,
        borderRadius: 40,
        borderWidth: 3,
        borderColor: '#c832fa',
    },
    image: {
        borderRadius: 40,
        borderWidth: 3,
        borderColor: '#ffffff',
    },
});

export default styles;