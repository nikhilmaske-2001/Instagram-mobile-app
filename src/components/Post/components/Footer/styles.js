import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        margin: 5,
    },
    likesCount: {
        fontWeight: 'bold',
        margin: 3,

    },
    caption: {
        margin: 3,
    },
    postedAt: {
        margin: 3,
        color: '#8c8c8c',
    },
});

export default styles;